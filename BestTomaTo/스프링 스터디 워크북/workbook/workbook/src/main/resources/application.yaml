spring:
  application:
    name: workbook

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/umc
    username: root
    password: jin315130*

  jpa:
    hibernate:
      ddl-auto: create
    defer-datasource-initialization: true
    properties:
      hibernate:
        format_sql: true
        # show-sql: true
  sql:
    init:
      mode: always


logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace
